apiVersion: apps/v1
kind: Deployment
metadata: { name: credit-score-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: credit-score-service } }
  template:
    metadata: { labels: { app: credit-score-service } }
    spec:
      containers:
        - name: credit-score-service
          image: credit-score-service:local
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8082 }]
          env:
            - { name: EUREKA_URL, value: "http://service-registry:8761/eureka/" }
            - { name: RABBIT_HOST, value: "rabbitmq" }
            - { name: REDIS_HOST, value: "redis" }
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8082 }
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata: { name: credit-score-service }
spec:
  selector: { app: credit-score-service }
  ports: [{ port: 8082, targetPort: 8082 }]
