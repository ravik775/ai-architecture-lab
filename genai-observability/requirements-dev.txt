# Dev/test-only dependencies - not needed to run the service, only to run
# the test suite in tests/ and lint the codebase. Install alongside
# requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==8.3.3
PyYAML==6.0.2
ruff==0.16.1
